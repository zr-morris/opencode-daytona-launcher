# Required: Daytona API key used to create sandboxes on demand.
DAYTONA_API_KEY=
# Optional: persisted into each sandbox so OpenCode can use OpenAI without a prompt.
OPENAI_API_KEY=
# Render injects PORT automatically; default 3000 for local runs.
PORT=3000
